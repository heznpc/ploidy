apiVersion: v2
name: ploidy
description: |
  Cross-session multi-agent debate MCP server. Same model, different context
  depths, better decisions. Single-replica chart because the state backend
  is a local SQLite file; horizontal scaling requires a different backend.
type: application
version: 0.1.0
appVersion: "0.3.3"
home: https://github.com/heznpc/ploidy-research
sources:
  - https://github.com/heznpc/ploidy-research
keywords:
  - mcp
  - multi-agent
  - debate
  - llm
maintainers:
  - name: heznpc
